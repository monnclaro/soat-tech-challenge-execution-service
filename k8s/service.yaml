apiVersion: v1
kind: Service

metadata:
  name: soat-execucao-api-service
  namespace: soat-execucao

spec:
  selector:
    app: soat-execucao-api

  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
      nodePort: 30083

  type: NodePort
